Place optional bundled service executables here before running `npm run dist:dmg`.

Supported executable names:

- `evalscope-service`
- `opencompass-service`

When present, the Electron shell starts these services on free local ports and injects their URLs into the EvalScope/OpenCompass tabs.

