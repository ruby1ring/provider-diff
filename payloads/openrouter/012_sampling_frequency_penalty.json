{
  "case_id": "or_sampling_frequency_penalty",
  "title": "frequency_penalty should be accepted",
  "category": "sampling",
  "parameters": ["frequency_penalty"],
  "method": "POST",
  "path": "/chat/completions",
  "payload": {
    "model": "openai/gpt-4o-mini",
    "messages": [
      {
        "role": "user",
        "content": "Write one sentence without repeating words."
      }
    ],
    "frequency_penalty": 0.3
  },
  "expect": {
    "http_status": 200,
    "required_response_fields": ["id", "object", "created", "model", "choices", "system_fingerprint"],
    "notes": "OpenRouter ChatRequest OpenAPI includes frequency_penalty."
  }
}
