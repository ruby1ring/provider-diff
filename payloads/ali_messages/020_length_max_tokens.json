{
  "case_id": "am_length_max_tokens",
  "title": "max_tokens 应限制 Messages 输出长度",
  "category": "length",
  "parameters": [
    "max_tokens"
  ],
  "method": "POST",
  "path": "/v1/messages",
  "payload": {
    "model": "deepseek-v4-pro",
    "max_tokens": 32,
    "messages": [
      {
        "role": "user",
        "content": "Explain compatibility testing in one sentence."
      }
    ],
    "thinking": {
      "type": "disabled"
    }
  },
  "expect": {
    "http_status": 200,
    "required_response_fields": [
      "id",
      "type",
      "role",
      "content",
      "model"
    ],
    "notes": "Current test config has DashScope Messages access to deepseek-v4-pro, so this case uses it for API-level compatibility validation. 关闭 thinking 后验证该参数，避免 DashScope Messages 将 reasoning token 计入 usage.output_tokens。"
  }
}
