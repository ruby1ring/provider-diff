{
  "case_id": "ali_sampling_top_k_integer",
  "title": "top_k 数值采样",
  "category": "sampling",
  "parameters": [
    "top_k"
  ],
  "method": "POST",
  "path": "/v1/chat/completions",
  "payload": {
    "model": "deepseek-v4-pro",
    "messages": [
      {
        "role": "user",
        "content": "列出两个测试用例命名原则。"
      }
    ],
    "top_k": 20
  },
  "expect": {
    "http_status": 200,
    "support_conclusion": "supported",
    "required_response_fields": [
      "id",
      "object",
      "created",
      "model",
      "choices",
      "usage"
    ],
    "provider_extension": true,
    "notes": "top_k 是非 OpenAI 标准参数；DeepSeek/Kimi/MiniMax 系列不支持。 Current test config has DashScope access to deepseek-v4-pro, so this case uses it for API-level compatibility validation."
  }
}
