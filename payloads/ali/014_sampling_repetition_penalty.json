{
  "case_id": "ali_sampling_repetition_penalty",
  "title": "repetition_penalty 重复度惩罚",
  "category": "sampling",
  "parameters": [
    "repetition_penalty"
  ],
  "method": "POST",
  "path": "/v1/chat/completions",
  "payload": {
    "model": "deepseek-v4-pro",
    "messages": [
      {
        "role": "user",
        "content": "用不重复的措辞写一句测试提示。"
      }
    ],
    "repetition_penalty": 1.05
  },
  "expect": {
    "http_status": 200,
    "support_conclusion": "supported",
    "required_response_fields": [
      "id",
      "object",
      "created",
      "model",
      "choices",
      "usage"
    ],
    "provider_extension": true,
    "notes": "repetition_penalty 是非 OpenAI 标准参数，文档要求大于 0。 Current test config has DashScope access to deepseek-v4-pro, so this case uses it for API-level compatibility validation."
  }
}
