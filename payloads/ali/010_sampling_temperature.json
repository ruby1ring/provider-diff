{
  "case_id": "ali_sampling_temperature",
  "title": "temperature 采样",
  "category": "sampling",
  "parameters": [
    "temperature"
  ],
  "method": "POST",
  "path": "/v1/chat/completions",
  "payload": {
    "model": "deepseek-v4-pro",
    "messages": [
      {
        "role": "user",
        "content": "生成一句简短的测试文案。"
      }
    ],
    "temperature": 0.2
  },
  "expect": {
    "http_status": 200,
    "support_conclusion": "supported",
    "required_response_fields": [
      "id",
      "object",
      "created",
      "model",
      "choices",
      "usage"
    ],
    "notes": "temperature 文档范围为 [0, 2)。 Current test config has DashScope access to deepseek-v4-pro, so this case uses it for API-level compatibility validation."
  }
}
