{
  "case_id": "ali_protocol_stream_basic",
  "title": "stream 基础流式返回",
  "category": "protocol",
  "parameters": [
    "stream"
  ],
  "method": "POST",
  "path": "/v1/chat/completions",
  "payload": {
    "model": "deepseek-v4-pro",
    "messages": [
      {
        "role": "user",
        "content": "请用流式方式逐句输出 3 到 5 句简短中文问候，不要一次性全部给出。"
      }
    ],
    "stream": true,
    "max_tokens": 128
  },
  "expect": {
    "http_status": 200,
    "support_conclusion": "supported",
    "response_mode": "sse",
    "stream_incremental": true,
    "stream_probe_attempts": 3,
    "required_chunk_fields": [
      "id",
      "object",
      "created",
      "model",
      "choices"
    ],
    "notes": "流式 case 验证 SSE/chunk 结构与增量分片（至少 2 个含 content 的 chunk）；同一 case 重复探测 3 次，任一次命中伪流式（单 chunk、长延迟后一次性返回）即 fail。Current test config has DashScope access to deepseek-v4-pro, so this case uses it for API-level compatibility validation."
  }
}
