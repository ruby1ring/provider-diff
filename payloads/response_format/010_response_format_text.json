{
  "case_id": "response_format_text",
  "title": "response_format=text · 渠道应接受 text 输出格式",
  "category": "output",
  "parameters": ["response_format"],
  "method": "POST",
  "path": "/chat/completions",
  "payload": {
    "model": "gpt-4o-mini",
    "messages": [
      {
        "role": "user",
        "content": "Reply with one plain text sentence."
      }
    ],
    "response_format": {
      "type": "text"
    },
    "max_completion_tokens": 1024
  },
  "expect": {
    "axis": "accept",
    "notes": "验证 response_format.type=text 是否被渠道接受。 预算须留足思考 token：默认开思考的模型（如 qwen3.8-max）reasoning 与正文共用 max_tokens 预算，预算过小会让正文被截断成空串，导致 response_format 断言随机失败——这不是渠道问题，别把这里改小。"
  }
}
